// Re-export du build généré. Source : `dist/tokens.ts`.
// Pendant le dev, importer directement depuis dist/ (le path mapping de tsconfig pointe vers dist).
export * from "../dist/tokens";
export { default } from "../dist/tokens";
